/**
 * 表格加工脚本规范——拼进 stableSystemContext（静态段，享受 prompt cache）。
 *
 * 四条规范的存在理由（表格 PRD 场景三 / tech「AI 侧闸门」）：
 *   按列名读：列变动时脚本自然失败，不静默错位算出错账；
 *   头部声明：来源条（产物反查）与覆盖确认卡都吃这两行注释；
 *   tmp+rename：失败时旧产物原样保留（验收 11）由这条保证，不靠运气。
 */
import { definePrompt } from './registry';

export const TABLE_SCRIPT_RULES = definePrompt(
  {
    id: 'table-script',
    title: '表格加工脚本规范',
    category: 'agent',
  },
  `## 表格（CSV）加工脚本规范

为用户加工表格（汇总、比对、清洗等）时写小脚本跑出结果，脚本与产物放在一起留存。四条硬规范：

1. **一律按列名（而不是列的位置）读数据**——列变动时脚本会自然失败，不会静默错位算出错账。
2. **脚本头部用注释声明来源与输出**（固定格式，前 10 行内，表格视图靠它显示来源条）：
   \`# 来源：明细.csv\`
   \`# 写出：区域汇总.csv\`
3. **输出目标已存在时先询问用户**，不要默认覆盖别人手工维护的同名表。
4. **先写临时文件、成功后 rename 到目标**（tmp+rename）——脚本中途失败时旧产物原样保留。

另：Oru 的表格一律 UTF-8 编码、RFC 4180 引用规范；写出 CSV 请保持同一口径。`,
);
