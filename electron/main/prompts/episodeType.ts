import { definePrompt } from './registry';

export const EPISODE_TYPE_PROMPT = definePrompt(
  {
    id: 'episode-type',
    title: 'Episode 类型分类法',
    category: 'memory',
  },
  `# Episode 类型分类法

可保存的情景记忆分五类。

- **user**：用户的稳定事实与特质——角色、专长、工作背景、目标，**也包括品味、爱好、健康状况、重要关系、生活中的持续事件**。用户的生活面与工作面同等重要。用户透露"我是谁 / 我怎么生活与工作"时用这类。

- **feedback**：用户给出的**跨任务复用**的协作规则。纠正（"别做 X"）和确认（"对，就这么做"）都要存；用户给了原因就一并记下——知道为什么，将来才能裁边界情况。**认作用域**：对某一份具体产物 / 文件的一次性要求（"这份 PPT 图配大点"）不是协作规则，是任务状态，不抓；只有泛化成跨任务的工作方式（"我做 PPT 都要图当主角"）才是 feedback。

- **project**：进行中的项目、计划或事件——谁在做什么、定了什么、为什么、到什么时候。项目维度的 episode 存在 \`projects/<id>/episodes/\` 下，projectId 由文件位置隐含，不单独存字段。跨项目的洞察归 agent 维度（\`twin/episodes/\`）。

- **reference**：信息在外部系统的位置指针——链接、文档、仪表盘、别处仓库的路径。存下来让将来的你知道**去哪找**。

- **agent**（Oru 特有）：你（Oru）自身演化的事实——表达风格的变化、内化的工作原则、对自己改变的观察。**区别于 user**（那是关于人类的，不是关于你的），**区别于 feedback**（那是用户下的指令；agent 记忆是你对已发生变化的自省）。

## 歧义裁决（按序判断）

先过留存门：对照下方"不保存的内容"，命中就不抓；过了门再按序判类型——下面这棵树默认输入已够格长期记、只决定归哪类，别用它给"其实不该存"的内容找一类塞进去。

1. 用户下了协作规则？→ **feedback**
2. 用户透露了自己的稳定事实？→ **user**
3. 你（Oru）在自省自己的变化、且无用户直接指令？→ **agent**
4. 信息主要是某个项目的状态？→ **project**
5. 信息是外部系统的指针？→ **reference**

一句话既像 user 又像 feedback（如"我讨厌啰嗦的文档"）：隐含协作指令时取 **feedback**；只是单纯特质、没有"所以你要怎样"时取 **user**。

## 不保存的内容

- **assistant 自己的现场发挥**——观点、推演、影评书评式的展开；即兴输出不沉淀（你自身已稳定的演化是 agent 类，不在此列）。
- **一次性问答**——用户问个做法、查个事实，答完即结束，不沉淀。
- **对话内的短期约定与进行中状态**——"读完回来聊""回头试试看"这类微约定过期快、失效不可感知，不记；正在进行的具体任务状态同理（跨对话仍成立的持续事件归 user，不在此列）。
- **寒暄与情绪的即时表达**——即时即逝，不构成对用户的持续理解。

例外有二：用户**明确要求记住**的照记——显式意愿优先；命中以上任何一条但**透露了稳定特质或持续事件**的，按对应类型抓。
`,
);
